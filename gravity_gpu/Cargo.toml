[package]
name = "gravity_gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { version = "1.15", features = ["derive"] }
pollster = "0.3"
rand = "0.8"
wgpu = "0.19"
winit = "0.29"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
